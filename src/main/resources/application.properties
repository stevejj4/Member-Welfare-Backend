spring.datasource.url=jdbc:mysql://localhost:3306/sunwelfaredb
spring.datasource.username=root
spring.datasource.password=Adm!n2026
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect

server.port=9090

# Application
# Application
app.name=SUN Welfare
app.frontend.login-url=${FRONTEND_LOGIN_URL:http://localhost:5173/login}
app.cors.allowed-origins=${CORS_ALLOWED_ORIGINS:http://localhost:5173}
app.auth.cookie.secure=${AUTH_COOKIE_SECURE:false}
app.auth.cookie.same-site=${AUTH_COOKIE_SAME_SITE:Lax}
app.mail.enabled=${MAIL_ENABLED:true}
app.mail.from=${MAIL_FROM:jumaomondi16@gmail.com}

# JWT (set JWT_SECRET in production; secret must be at least 32 characters for HS256)
app.jwt.secret=${JWT_SECRET:mySuperSecretKeyForJwtGeneration1234567890}
app.jwt.access-expiration-ms=${JWT_ACCESS_EXPIRATION_MS:900000}
app.jwt.refresh-expiration-ms=${JWT_REFRESH_EXPIRATION_MS:604800000}

# SMTP ? Gmail setup
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=jumaomondi16@gmail.com
spring.mail.password=vjbp tlqn wqpc pjrx
spring.mail.default-encoding=UTF-8
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.connectiontimeout=10000
spring.mail.properties.mail.smtp.timeout=10000
spring.mail.properties.mail.smtp.writetimeout=10000
